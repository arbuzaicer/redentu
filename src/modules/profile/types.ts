export type Profile = {
  name: string;
  sureName: string;
  photo_url: string;
  linked_url: string;
  email: string;
};

export type ProfileData = {
  name: string | null;
  sureName: string | null;
  email: string | null;
};
